'use strict';

var createSlots = require('../utils/create-slots.js');

const {
  Slots,
  Slot
} = createSlots(['Caption', 'Label', 'LeadingVisual', 'Validation']);

exports.Slot = Slot;
exports.Slots = Slots;
