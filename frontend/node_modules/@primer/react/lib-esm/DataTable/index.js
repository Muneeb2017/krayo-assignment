import { Table as Table$1, TableContainer, TableTitle, TableSubtitle, TableActions, TableDivider, TableHead, TableBody, TableHeader, TableRow, TableCell, TableCellPlaceholder } from './Table.js';

const Table = Object.assign(Table$1, {
  Container: TableContainer,
  Title: TableTitle,
  Subtitle: TableSubtitle,
  Actions: TableActions,
  Divider: TableDivider,
  Head: TableHead,
  Body: TableBody,
  Header: TableHeader,
  Row: TableRow,
  Cell: TableCell,
  CellPlaceholder: TableCellPlaceholder
});

export { Table };
